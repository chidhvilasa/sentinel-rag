# Screenshots

Placeholder directory for web demo screenshots referenced from the top-level README (e.g. `demo-home.png`, `demo-analysis.png`). Add PNGs/GIFs here and update the corresponding `![...]()` links in `README.md`.
